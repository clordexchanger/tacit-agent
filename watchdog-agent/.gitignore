# dependencies
   node_modules/

   # next.js build output
   .next/
   out/

   # environment variables — never commit these, they contain real secrets
   .env
   .env.local
   .env.*.local

   # vercel
   .vercel/

   # misc
   .DS_Store
   *.log
